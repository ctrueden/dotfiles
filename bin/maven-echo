#!/bin/sh

# maven-echo - an easy way to evaluate POM expressions.
#
# Example:
#    maven-echo 'group=${project.groupId}, artifact=${project.artifactId}'

arg=$1
shift
mvn -q exec:exec -Dexec.executable=echo -Dexec.args="$arg" $@
